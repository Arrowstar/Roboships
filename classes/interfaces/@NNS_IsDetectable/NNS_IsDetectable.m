classdef(Abstract = true) NNS_IsDetectable < matlab.mixin.SetGet 
    %NNS_IsDetectable Marker interface to show if a prop object is
    %detectable by a sensor.  May also be used for determining if a
    %particular sensor can detect the prop object.

    
    properties
        
    end
    
    methods
        
    end
end

