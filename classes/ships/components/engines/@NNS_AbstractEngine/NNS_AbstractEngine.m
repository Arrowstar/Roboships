classdef(Abstract = true) NNS_AbstractEngine < NNS_VehicleComponent
    %NNS_AbstractEngine An abstract class for 'engines', classes that
    %provide linear motion to a game object.
    
    properties
        
    end
    
    methods
        curSpeed = getCurrentThrust(obj)
    end
end